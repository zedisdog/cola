package controllers

import (
	"{{moduleName}}/internal/database"
	"github.com/gin-gonic/gin"
)

func {{name}}(cxt *gin.Context) {
	_, _ = database.GetInstance()
	cxt.JSON(200, gin.H{"success": true})
}
