package storage

import (
	"github.com/spf13/viper"
	"github.com/zedisdog/cola/storage"
	"sync"
)

var instance *storage.Storage
var once sync.Once

func GetInstance() *storage.Storage {
	once.Do(func() {
		instance = storage.NewByViper(viper.GetViper())
	})
	return instance
}
