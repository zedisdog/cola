package queue

import (
	"github.com/spf13/viper"
	"github.com/zedisdog/cola/task"
)

var Queue *task.Queue

func init() {
	if viper.GetBool("queue.enable") {
		Queue = task.NewQueue(50)
		Queue.Start()
		return
	}
}
